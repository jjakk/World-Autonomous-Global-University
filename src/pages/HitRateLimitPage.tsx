function HitRateLimitPage() {
    return (
        <>
         Looks like you've hit the rate limit for API calls. Please wait a moment and try again. & swap your API key out for a new one
        </>
    );
}

export default HitRateLimitPage;